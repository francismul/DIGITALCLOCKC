name: Build and Release Digital Clock

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install MinGW (GCC for Windows)
        uses: egor-tensin/setup-mingw@v2
        with:
          platform: x64

      - name: Build executable
        run: gcc -o digitalclock.exe digital_clock_win32.c -mwindows -luser32 -lgdi32

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: digitalclock-exe
          path: digitalclock.exe

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: digitalclock-exe
          path: dist

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ github.token }}
          files: dist/digitalclock.exe
